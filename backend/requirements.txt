# ============================================================================
# LEGACY (Flask - Optional for backward compatibility)
# ============================================================================
Flask==3.0.0
Werkzeug==3.0.0

# ============================================================================
# NEW ARCHITECTURE (FastAPI + Pydantic)
# ============================================================================
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.10.0
pydantic-settings==2.3.0

# ============================================================================
# DATABASE (PostgreSQL ORM)
# ============================================================================
sqlalchemy>=2.0.30  # Python 3.13 compatible
psycopg2-binary>=2.9.10
alembic>=1.13.1

# ============================================================================
# UTILITIES
# ============================================================================
python-dotenv==1.0.0
python-multipart>=0.0.6  # File upload support for FastAPI

# ============================================================================
# CONTENT GENERATION (Hybrid Neuro-Symbolic Architecture)
# ============================================================================
sympy==1.12  # Symbolic mathematics for deterministic skeletons
jinja2>=3.1.4  # Templating for rendering questions
httpx>=0.27.0  # Async HTTP client for LLM API calls
instructor>=1.7.0  # Pydantic-powered structured outputs (Python 3.13 compatible)
pyyaml>=6.0.1  # YAML parsing for question bank files

# ==========================================================================
# NOTION CMS + CACHING
# ==========================================================================
# (Removed in Phase 0 cleanup)

